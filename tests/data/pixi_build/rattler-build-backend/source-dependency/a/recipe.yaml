package:
  name: a
  version: 0.1.0

build:
  script:
    - if: win
      then:
        - mkdir -p "%PREFIX%"
        - echo hello from package a! > "%PREFIX%\hi"
      else:
        - mkdir -p $PREFIX
        - echo "hello from package a!" > $PREFIX/hi
